name: Release Eum Server

on: [push]

jobs:
    build_and_test_eum_server:
        runs-on: ubuntu-latest
        container: openjdk:8-jdk
        
        steps:
            - uses: actions/checkout@v3 
            - run: chmod +x gradlew
            - run: ./gradlew test
    